[package]
name = "kv-database"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3.3"
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
tempfile = "3.10"
serde_json = "1.0"
